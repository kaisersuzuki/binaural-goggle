# OpenSCAD Source

Place `enclosure_v7.scad` here when ready.

The STL files in `../stl/` were exported from this source. Keeping the SCAD here in version control means future revisions can be parametric edits rather than starting from scratch.
